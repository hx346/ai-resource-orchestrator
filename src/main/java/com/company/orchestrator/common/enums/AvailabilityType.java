package com.company.orchestrator.common.enums;

/** 员工可用性类型 / Employee availability type. */
public enum AvailabilityType {
    /** 可用 / Available */
    AVAILABLE,
    /** 休假 / Leave */
    LEAVE,
    /** 培训 / Training */
    TRAINING,
    /** 出差 / Business trip */
    BUSINESS_TRIP,
    /** 不可安排 / Blocked */
    BLOCKED
}
